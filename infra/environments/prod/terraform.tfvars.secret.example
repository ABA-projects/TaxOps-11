# Copia este archivo a "terraform.tfvars.secret" (ese nombre está en .gitignore, NUNCA se commitea)
# y rellena los valores reales. Terraform lo usa con: terraform plan -var-file=terraform.tfvars.secret

secrets = {
  DATABASE_URL             = "postgresql://usuario:password@host.neon.tech/neondb?sslmode=require"
  SECRET_KEY               = "" # JWT signing key — genera una nueva, no reuses la de Cloud Run
  GROQ_API_KEY              = ""
  GOOGLE_CLIENT_ID          = ""
  GOOGLE_CLIENT_SECRET      = ""
  TAXOPS_SUPERADMIN_EMAILS  = "jaimehenao8126@outlook.com,felipe.arbe08@gmail.com"
  BOOTSTRAP_SECRET          = ""
}
